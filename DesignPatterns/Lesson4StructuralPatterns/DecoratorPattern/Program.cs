using DecoratorPattern;
using DecoratorPattern.NotifierApp;

//AbstractRenderer renderer = new Renderer();

//renderer.RenderInformation("Hakuna Matata");
//renderer = new ColorDecorator(renderer, ConsoleColor.Magenta);
//renderer = new UpperCaseDecorator(renderer);

//renderer.RenderInformation("Hello kitty");

//Notifier notifier = new SmsDecorator();
//notifier = new FacebookDecorator(notifier);
//notifier = new SlackDecorator(notifier);

//notifier.Send("Hello Kitty");
